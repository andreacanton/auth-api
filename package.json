{
  "name": "auth-api",
  "version": "0.0.0",
  "scripts": {
    "start": "NODE_ENV=development && bun --watch run src/index.ts",
    "twc": "NODE_ENV=test && bun test --watch --coverage",
    "db:migrate": "bun run src/migrate.ts",
    "db:seeds": "bun run src/seeds.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.28.6",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.19.13"
  },
  "module": "src/index.js"
}